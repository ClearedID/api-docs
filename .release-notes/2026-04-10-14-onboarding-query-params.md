# Release notes

## Added

- **Onboarding Pages doc:** Public URL query parameters (`emailAddress`, OTP behaviour, `firstName` / `lastName` / `phoneNumber`, lead-capture `queryParamKey`).

## Fixed

- (n/a)

## Removed

- (n/a)
