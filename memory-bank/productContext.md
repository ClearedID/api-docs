# Product context — api-docs

Integrators use these docs to wire REST APIs and webhooks. Webhook naming for onboarding is camelCase in the runtime catalog (`identityVerificationCleared`, `initialReviewCompleted`); signing docs use snake_case.
