# Product context — api-docs

Integrators use these docs to wire REST APIs and webhooks. Webhook naming for onboarding is camelCase in the runtime catalog (`identityVerificationCleared`, `initialReviewCompleted`); signing docs use snake_case. Due Diligence is a case status on `initialReviewCompleted`, not a separate catalog event.
